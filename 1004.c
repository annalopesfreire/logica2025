#include <stdio.h>

int main() {
    int A, B, PROD;

    // Lê dois valores inteiros da entrada
    scanf("%d", &A);
    scanf("%d", &B);

    // Calcula o produto
    PROD = A * B;

    // Imprime o resultado no formato especificado
    printf("PROD = %d\n", PROD);

    return 0;
}
